const inputs = {
    INPUT_NAME: {
        TYPE: "text",
        NAME: "name",
        PLACEHOLDER: "Please enter your name...",
        PATTERN: "^[a-zA-Zа-яА-Я]+(([' -][a-zA-Zа-яА-Я ])?[a-zA-Zа-яА-Я]*)*$"
    }
}

export { inputs };